package main

import cos, sin from Math
import cos, sin from Math
import cos, sin from Math
import cos, sin from Math
function main:
    var int64 x
    var x = 2
    if x > 1:
        println("x is greater than 1")
    else